{
  "name": "shutter-store",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "preinstall": "cd client && npm install && cd ..",
    "prestart": "cd client && npm run build && cd ..",
    "test": "mocha",
    "start": "node app.js"
  },
  "dependencies": {
    "cookie-parser": "~1.4.3",
    "debug": "~2.6.9",
    "express": "~4.16.0",
    "morgan": "~1.9.0",
    "mongodb": "^3.2.3",
    "assert": "^2.0.0",
    "md5.js": "^1.3.5"
  },
  "devDependencies": {
    "mocha": "^6.1.4",
    "sinon": "^7.3.2",
    "chai": "^4.2.0"
  }
}
